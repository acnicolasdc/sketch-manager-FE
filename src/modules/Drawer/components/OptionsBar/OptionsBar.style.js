import { styled } from 'baseui';

export const Option = styled('div', {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '80px',
    width:'80px',
    backgroundColor: 'red'
});